<!DOCTYPE html>
<html>
  <head>
    <title>Dog Explorer</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Bootstrap Styles -->
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container">
        <%= link_to "🐾 Dog Explorer", root_path, class: "navbar-brand fw-bold" %>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item"><%= link_to "Owners", owners_path, class: "nav-link" %></li>
            <li class="nav-item"><%= link_to "Dogs", dogs_path, class: "nav-link" %></li>
            <li class="nav-item"><%= link_to "Breeds", breeds_path, class: "nav-link" %></li>
            <li class="nav-item"><%= link_to "Traits", traits_path, class: "nav-link" %></li>
            <li class="nav-item"><%= link_to "About", about_path, class: "nav-link" %></li>
          </ul>
        </div>
      </div>
    </nav>

    <!-- Page Content -->
    <div class="container mt-4">
      <%= yield %>
    </div>

    <!-- Footer -->
    <footer class="bg-dark text-light text-center py-3 mt-5">
      <p class="mb-0">&copy; <%= Time.now.year %> Dog Explorer. Built with ❤️ using Ruby on Rails.</p>
    </footer>
  </body>
</html>
