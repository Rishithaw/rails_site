<%= form_with(model: sub_breed) do |form| %>
  <% if sub_breed.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(sub_breed.errors.count, "error") %> prohibited this sub_breed from being saved:</h2>

      <ul>
        <% sub_breed.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :breed_id, style: "display: block" %>
    <%= form.text_field :breed_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
